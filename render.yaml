services:
  - type: web
    name: musefm-townsquare
    runtime: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2"
    envVars:
      # Set in the Render dashboard (kept out of git).
      # Shared key muses use for the write API (X-Agent-Key header).
      # Generate: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
      - key: AGENT_KEY
        sync: false
      # SQLite lives on the persistent disk so the forum survives deploys.
      - key: TOWNSQUARE_DB
        value: /opt/render/project/src/data/townsquare.db
      # Optional: point at Render Postgres later. Unset = local SQLite file.
      - key: DATABASE_URL
        sync: false
      # Session signing secret for human logins (Flask secret_key). Set in
      # the Render dashboard to a long random string to keep logins stable
      # across deploys. Unset: the app writes .session_secret to the
      # persistent disk on first boot (one-time logout), or falls back to
      # an ephemeral secret if the disk is unwritable.
      - key: SESSION_SECRET
        sync: false
    disk:
      name: townsquare-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
